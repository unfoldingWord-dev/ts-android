apply plugin: 'com.android.library'
android {
    compileSdkVersion 25

    defaultConfig {
        minSdkVersion 15
        targetSdkVersion 25
    }

    // Do not abort build if lint finds errors
    lintOptions {
        abortOnError false
    }
}